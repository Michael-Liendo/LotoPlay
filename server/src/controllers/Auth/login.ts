import type { FastifyRequest } from 'fastify';
import type { Reply } from '../../types/Reply';

export default async function login(_request: FastifyRequest, _reply: Reply) {
  // TODO: Implement login
}
